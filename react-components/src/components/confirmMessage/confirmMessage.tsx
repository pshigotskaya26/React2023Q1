import React, { Component } from 'react';
import './index.css';

class ConfirmMessage extends Component {
  render() {
    return <p className="confirm-message">The card is added in the list.</p>;
  }
}

export default ConfirmMessage;
