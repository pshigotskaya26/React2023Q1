import React from 'react';
import './index.css';
const ConfirmMessage = () => {
    return (React.createElement("p", { className: "confirm-message", "data-testid": "confirm-message" }, "The card is added in the list."));
};
export default ConfirmMessage;
